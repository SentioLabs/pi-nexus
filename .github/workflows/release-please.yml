name: Release Please

on:
  push:
    branches:
      - main

permissions:
  contents: write
  pull-requests: write
  id-token: write

jobs:
  release:
    name: Release and publish
    runs-on: ubuntu-latest
    env:
      NPM_CONFIG_LEGACY_PEER_DEPS: "true"

    steps:
      - name: Release Please
        id: release
        uses: googleapis/release-please-action@v4
        with:
          config-file: release-please-config.json
          manifest-file: .release-please-manifest.json

      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          registry-url: https://registry.npmjs.org
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Publish pi-arc to npm
        run: node scripts/npm-publish-workspace-if-needed.mjs @sentiolabs/pi-arc

      - name: Publish pi-code-quality to npm
        run: node scripts/npm-publish-workspace-if-needed.mjs @sentiolabs/pi-code-quality

      - name: Publish pi-frontend-design to npm
        run: node scripts/npm-publish-workspace-if-needed.mjs @sentiolabs/pi-frontend-design

      - name: Publish pi-scriptable-statusline to npm
        run: node scripts/npm-publish-workspace-if-needed.mjs @sentiolabs/pi-scriptable-statusline
